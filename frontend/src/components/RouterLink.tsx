/**
 * This file is simply used as an autocomplete helper for Visual Studio Code.
 *
 * Chakra UI exports a Link, which we use more than react-router-dom's Link,
 * so we re-export it as RouterLink.
 */

import { Link as RouterLink } from 'react-router-dom';
export default RouterLink;
